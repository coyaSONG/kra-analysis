# Ralph Progress Log
Started: 2026-04-19
---

## Patterns
- Best so far: 0.378641 at commit 2869a73 ("lower learning_rate to 0.03"). Beat this strictly to keep.
- Kept-mutation axis to date: only `min_samples_leaf` between 35–42, plus `learning_rate=0.03`. All other axes have been tried and discarded.
- Plateau region: 17 consecutive discards/crashes after iteration 6 — every mutation was a small tweak in the same hyperparameter neighborhood. To break the plateau, vary the *axis*, not the *value*.
- Verify takes minutes (10 seeds × full holdout). Do not abort early.
- Guard must pass before verify; revert immediately on guard failure.
- Removing `draw_rr` alone regressed to 0.375405, so simple one-feature ablations on the draw-position signal are not a promising plateau-breaker.

## Prior axes already tried (all discarded after baseline-keep era)
- l2_regularization: 0.4, 0.5
- max_iter: 600, 800
- max_depth: 7
- min_samples_leaf: 25, 43, 45 (only 35/40/42 kept)
- positive_class_weight: 0.9, 0.95, 1.05 (with and without synced common weight)
- These hyperparameter dials are exhausted within the current feature/model choice.

## Suggested unexplored axes for next iterations
- Add or remove a feature in `clean_model_config.json` `features` list
- Switch model class (if config schema allows)
- Change training data window
- Change class balancing strategy (sample weights, threshold)
- Adjust evaluation seeds or holdout window via config (if writable)

---
## [2026-04-19T18:38:40+0900] - iteration 24
- mutation: remove `draw_rr` from `features` atop `positive_class_weight=1.05` and `learning_rate=0.03`
- guard: pass
- metric: 0.375405  (best so far: 0.378641)
- decision: discard
- learning: Removing the race-relative draw feature weakened worst-seed holdout performance, so this positional signal is still helping the current HGB setup. Do not retry the same single-feature ablation; next iteration should probe a different feature family or a broader feature-set change.
---
## [2026-04-26T01:02:08+0900] - iteration 38
- mutation: switch top-level `model.kind` and params from the kept HGB configuration to the schema-supported `rf_depth8_600trees` candidate.
- hypothesis: A bagged RandomForest candidate could improve worst-seed recent-holdout hit rate by reducing HGB-specific variance after a long plateau of HGB hyperparameter and single-feature regressions.
- guard result: pass (`18 passed`).
- metric and current best: 0.368932 vs current best 0.378641.
- decision: discard; metric regressed by -0.009709, so reverted experiment commit `5c4256e`.
- learning: The RF family is much slower and weaker than the kept HGB context on worst-seed holdout; avoid retrying RF depth8/600 trees and prefer another schema-supported model family or a coherent feature-family change next.
---
## [2026-04-26T02:43:36+0900] - iteration 39
- mutation: switch top-level `model.kind` and params from the kept HGB configuration to the schema-supported `et_depth8_800trees` candidate.
- hypothesis: An ExtraTrees candidate could improve worst-seed recent-holdout hit rate by reducing HGB-specific split variance with randomized thresholds after a long HGB plateau and an RF regression.
- guard result: pass (`18 passed`).
- metric and current best: 0.23301 vs current best 0.378641.
- decision: discard; metric regressed by -0.145631, so reverted experiment commit `8f7f571`.
- learning: ET depth8/800 trees is both very slow and much weaker than the kept HGB context; avoid this bagged-tree family/value pair and next prefer a different schema-supported family such as logreg or a coherent feature-family change.
---
## [2026-04-26T04:24:36+0900] - iteration 40
- mutation: switch top-level `model.kind` and params from the kept HGB configuration to the schema-supported `logreg_balanced_c075` candidate.
- hypothesis: A simpler balanced logistic-regression ranker could improve worst-seed recent-holdout hit rate by reducing tree-model variance after HGB hyperparameter, RF, and ET regressions.
- guard result: pass (`18 passed`).
- metric and current best: 0.291262 vs current best 0.378641.
- decision: discard; metric regressed by -0.087379, so reverted experiment commit `aece44b`.
- learning: The linear balanced logreg family is much weaker and very slow due to repeated non-convergence at `max_iter=2000`; avoid retrying `logreg_balanced_c075` without a materially different preprocessing/scaling context, and next prefer a coherent feature-family change or another non-model-family axis.
---
## [2026-04-26T06:08:04+0900] - iteration 41
- mutation: add the `career_depth` feature bundle (`hr_starts_y`, `hr_starts_t`, `horse_low_sample`) atop the current draw_rr-removed HGB context.
- hypothesis: Raw career start counts plus a low-sample flag could reduce overconfidence on lightly sampled horses and improve worst-seed recent-holdout stability after model-family switches failed.
- guard result: pass (`18 passed`).
- metric and current best: 0.368932 vs current best 0.378641.
- decision: discard; metric regressed by -0.009709, so reverted experiment commit `78497da`.
- learning: Adding raw career-depth context did not recover worst-seed performance and made verify much slower; avoid retrying this exact career_depth bundle in the current HGB/draw_rr-removed context, and next prefer a different coherent feature-family axis or a leakage-free split/window experiment.
---
## [2026-04-26T07:51:51+0900] - iteration 42
- mutation: add the `speed_shape` feature bundle (`wg_budam_rank`, `field_size_live`, `gap_3rd_4th`) atop the current draw_rr-removed HGB context.
- hypothesis: Live field size, burden-rank shape, and top-skill gap could capture same-race competitive structure and improve worst-seed recent-holdout stability after model-family switches and career-depth features failed.
- guard result: pass (`18 passed`).
- metric and current best: 0.355987 vs current best 0.378641.
- decision: discard; metric regressed by -0.022654, so reverted experiment commit `9d83e6d`.
- learning: The speed_shape bundle can produce strong individual seeds but worsens worst-seed stability through seed 02; avoid retrying this exact bundle in the current HGB/draw_rr-removed context, and next try a different single axis such as a leakage-free split/window experiment or a narrower retained-feature cleanup rather than adding more shape features.
---
## [2026-04-26T09:36:52+0900] - iteration 43
- mutation: change the split/window axis to train through `20251031`, evaluate dev on `20251101`-`20251130`, and keep `20251201+` as the recent holdout test.
- hypothesis: Adding October races to training while preserving a November dev window and December recent holdout could capture late-season form shifts and improve worst-seed stability after model-family and feature-family regressions.
- guard result: pass (`18 passed`).
- metric and current best: 0.375405 vs current best 0.378641.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `9cb8b07`.
- learning: Including October in training improved several seeds but hurt seed 09 enough to regress the worst-seed metric; avoid this exact October-expanded split, and next prefer a different single axis such as retained-feature cleanup or another leakage-free window change that does not simply expand training into October.
---
## [2026-04-27T02:17:16+0900] - iteration 1
- mutation: remove the direct stored-only skill feature family (`jk_skill`, `tr_skill`) from `features`, leaving the existing place-rate context in place.
- hypothesis: Direct `jk_skill`/`tr_skill` values are stored/backfill-risk and partly redundant with jockey/trainer place-rate features, so removing them may improve leakage-free worst-seed holdout stability while preserving the T-30 operational contract.
- guard result: pass (`57 passed`).
- metric and current best: 0.119741 vs prior clean-v2 current best 0.116505; new clean-v2 best 0.119741.
- decision: keep; metric improved by +0.003236 over the valid clean-v2 TSV baseline.
- learning: The contract-aligned direct-skill cleanup clears the clean-v2 baseline, but the result is still far below the invalidated historical plateau; continue from clean-v2 rows only and next test another release-safe feature-family cleanup or a coherent addition from the RELEASE bucket rather than reusing invalid historical rows for selection.
---
## [2026-04-27T05:44:06+0900] - iteration 2
- mutation: add the T-30 RELEASE/current_model_input body-weight delta feature (`weight_delta`) to `features`, leaving model kind, split, class weighting, and all other inputs unchanged.
- hypothesis: `weight_delta` should add release-safe prerace condition information beyond absolute `wgHr_value`, improving worst-seed recent-holdout stability without violating the T-30 operational contract.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs prior clean-v2 current best 0.119741; new clean-v2 best 0.122977.
- decision: keep; metric improved by +0.003236 over the prior valid clean-v2 best.
- learning: Body-weight delta is a productive release-safe feature addition in the clean-v2 context; next avoid null-only `past_stats` additions on this canonical snapshot unless the dataset is regenerated with past_stats coverage, and prefer another high-coverage T-30 release-safe feature or a focused cleanup around redundant weight/body-size signals.
---
## [2026-04-27T07:06:51+0900] - iteration 3
- mutation: add the T-30 RELEASE/current_model_input cancelled_changed_jockey context (`field_size_live`, `cancelled_count`) to `features`, leaving model kind, split, class weighting, and all other inputs unchanged.
- hypothesis: `field_size_live` and `cancelled_count` may add release-safe pre-cutoff live-field context beyond raw `field_size`, improving worst-seed recent-holdout stability without using odds or timing-unverified sources.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `c8a16b5` with `7ed768a`.
- learning: The cancelled/live-field context did not improve the clean-v2 worst-seed floor and made verify much slower; avoid retrying this exact `field_size_live` + `cancelled_count` addition in the current HGB/weight_delta context, and next prefer a different release-safe feature cleanup or a non-null high-coverage single axis.
---
## [2026-04-27T08:30:32+0900] - iteration 4
- mutation: remove the stored-only direct horse skill feature (`horse_top3_skill`) from `features`, leaving model kind, split, class weighting, `weight_delta`, and the remaining horse place-rate/start-count context unchanged.
- hypothesis: `horse_top3_skill` is a stored/backfill-risk direct skill value that may be redundant with retained horse place-rate and start-count signals, so removing it may improve leakage-free worst-seed holdout stability while preserving the T-30 operational contract.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `af1e5cf` with `81b4d94`.
- learning: Removing `horse_top3_skill` did not lift the clean-v2 worst-seed floor, so avoid this single direct-horse-skill cleanup in the current HGB/weight_delta context; next prefer a different single axis such as class weighting or a coherent retained-feature cleanup that is not another direct skill removal.
---
## [2026-04-27T09:53:25+0900] - iteration 5
- mutation: raise synchronized `positive_class_weight` from `1.00` to `1.05` in both `model` and `experiment.common_hyperparameters`, leaving model family, split/window, and features unchanged.
- hypothesis: A mild positive-class upweight may improve unordered top-3 recall and raise the worst-seed holdout floor in the clean-v2 kept context where direct jockey/trainer skill inputs were removed and `weight_delta` was added.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `d580499` with `123caaa`.
- learning: `positive_class_weight=1.05` does not improve the clean-v2 HGB/weight_delta context despite strong early seeds; avoid this exact class-weight value in the current context and next prefer a different single axis such as a release-safe feature cleanup/addition or a materially different class-weight setting only with a new kept context.
---
## [2026-04-27T11:17:00+0900] - iteration 6
- mutation: remove the stored-only annual horse start-count feature (`horse_starts_y`) from `features`, leaving model kind, split/window, class weighting, `weight_delta`, and the remaining horse skill/place-rate context unchanged.
- hypothesis: `horse_starts_y` may be a redundant/backfill-risk career-volume signal once horse place-rate and total place-rate context remain, so removing it could reduce seed-level overfit while preserving the T-30 operational contract.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `7460846` with `191ca35`.
- learning: Removing `horse_starts_y` does not lift the clean-v2 floor and made verify unusually slow; avoid this exact single stored horse-volume cleanup in the current HGB/weight_delta context, and next prefer a genuinely new axis such as another model-family candidate only if schema-supported or a coherent release-safe feature addition with demonstrated non-null coverage.
---
## [2026-04-27T12:40:52+0900] - iteration 7
- mutation: add the operationally allowed stored-as-of horse win-rate feature (`horse_win_rate`) to `features`, leaving model kind, split/window, class weighting, `weight_delta`, and all existing place-rate/top3-skill context unchanged.
- hypothesis: `horse_win_rate` could add a win-only, top-heavy horse-strength signal beyond retained place-rate and `horse_top3_skill` context, improving worst-seed recent-holdout stability while preserving the T-30 operational contract.
- guard result: pass (`57 passed`).
- metric and current best: 0.122977 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `8b7bf9c` with `c5fea29`.
- learning: Adding horse win-rate improved several seeds but seed 09 fell back to the clean-v2 floor, so avoid this single stored horse win-rate addition in the current HGB/weight_delta context; next try a genuinely new axis, preferably a coherent race-relative/core-card feature family or a split/window change rather than another isolated horse-history scalar.
---
## [2026-04-27T14:05:25+0900] - iteration 8
- mutation: add the release-safe same-race rating-relative core-card features (`rating_rank`, `rating_rr`) to `features`, leaving model kind, split/window, class weighting, `weight_delta`, and all other inputs unchanged.
- hypothesis: Same-race normalized rating structure could stabilize weak seeds by giving HGB explicit within-field strength ordering beyond raw `rating`, without using odds or timing-unverified sources.
- guard result: pass (`57 passed`).
- metric and current best: 0.119741 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `bbb04e4` with `493114a`.
- learning: Rating-relative core-card additions hurt the worst-seed floor, especially leaving seed 09 at 0.119741; avoid retrying this exact `rating_rank` + `rating_rr` bundle in the current HGB/weight_delta context and next prefer a different single axis such as a split/window change or a materially different release-safe feature family.
---
## [2026-04-27T15:35:00+0900] - iteration 9
- mutation: add the T-30 release-safe same-race draw-relative feature (`draw_rr`) to `features`, leaving model kind, split/window, class weighting, `weight_delta`, and all other inputs unchanged.
- hypothesis: Field-normalized draw position could stabilize weak seeds by giving HGB relative stall context beyond raw `draw_no` when field sizes vary, without using odds or timing-unverified sources.
- guard result: pass (`57 passed`).
- metric and current best: 0.119741 vs current best 0.122977; clean-v2 best remains 0.122977.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `75e0340` with `f066bf8`.
- learning: `draw_rr` did not recover the clean-v2 worst-seed floor and repeated the 0.119741 failure mode seen in rating-relative features; avoid adding isolated race-relative core-card transforms in the current HGB/weight_delta context, and next prefer a split/window change or a coherent high-coverage stored-as-of rate family rather than another single relative transform.
---
## [2026-04-27T16:57:46+0900] - iteration 10
- mutation: switch top-level `model.kind` and params from the kept HGB configuration to the schema-supported `rf_depth8_600trees` candidate, leaving split/window, synchronized `positive_class_weight`, and all features unchanged.
- hypothesis: A bagged RandomForest candidate could reduce HGB-specific seed variance and raise the leakage-free worst-seed floor in the clean-v2 context where direct jockey/trainer skill inputs were removed and `weight_delta` was added.
- guard result: pass (`57 passed`).
- metric and current best: 0.145631 vs prior clean-v2 current best 0.122977; new clean-v2 best 0.145631.
- decision: keep; metric improved by +0.022654 over the prior valid clean-v2 best.
- learning: The RF family is expensive but materially improves the clean-v2 worst-seed floor, so keep this model-family switch and next exploit the RF context with a cost-aware single-axis RF parameter or release-safe feature experiment rather than returning to isolated HGB race-relative feature additions.
---
## [2026-04-27T18:21:31+0900] - iteration 11
- mutation: raise active RF `min_samples_leaf` from 8 to 12, leaving model family, `n_estimators`, `max_depth`, split/window, class weighting, and features unchanged.
- hypothesis: Stronger RF leaf regularization could reduce seed-level variance and raise the leakage-free worst-seed floor after the RF model-family switch improved the clean-v2 best.
- guard result: pass (`57 passed`).
- metric and current best: 0.142395 vs current best 0.145631; clean-v2 best remains 0.145631.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `8958f4b` with `f41326c`.
- learning: Increasing RF leaf size worsened the seed 05 floor while leaving the mean roughly unchanged, so avoid `min_samples_leaf=12` in the current RF/weight_delta context; next prefer a different RF axis such as depth/tree count or a release-safe feature experiment rather than stronger leaf regularization.
---
## [2026-04-27T19:44:39+0900] - iteration 12
- mutation: raise active RF `max_depth` from 8 to 10, leaving model family, `n_estimators`, `min_samples_leaf`, split/window, class weighting, and features unchanged.
- hypothesis: Deeper RF trees could capture additional prerace feature interactions while retaining leaf-size regularization, improving worst-seed recent-holdout stability after the RF model-family switch improved the clean-v2 best.
- guard result: pass (`57 passed`).
- metric and current best: 0.135922 vs current best 0.145631; clean-v2 best remains 0.145631.
- decision: discard; metric regressed by -0.009709, so reverted experiment commit `4394a8f` with `b2ecd08`.
- learning: Increasing RF depth was much slower and weakened the worst-seed floor, so avoid `max_depth=10` in the current RF/weight_delta context; next prefer a cost-aware RF tree-count/depth reduction or a coherent release-safe feature experiment rather than making RF trees deeper.
---
## [2026-04-27T21:08:31+0900] - iteration 13
- mutation: lower active RF `min_samples_leaf` from 8 to 6, leaving model family, `n_estimators`, `max_depth`, split/window, class weighting, and features unchanged.
- hypothesis: A slightly smaller RF leaf size could recover useful prerace feature interactions without increasing tree depth, raising the leakage-free worst-seed floor after the RF model-family switch improved the clean-v2 best.
- guard result: pass (`57 passed`).
- metric and current best: 0.142395 vs current best 0.145631; clean-v2 best remains 0.145631.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `abab730` with `b8d5cb5`.
- learning: Both directions away from RF `min_samples_leaf=8` have now regressed the clean-v2 floor, so avoid further leaf-size tweaks in the current RF/weight_delta context; next prefer a different RF axis such as tree count or a coherent release-safe feature experiment.
---
## [2026-04-27T22:32:56+0900] - iteration 14
- mutation: switch top-level `model.kind` and params from the kept RF configuration to the schema-supported `et_depth8_800trees` candidate, leaving split/window, synchronized `positive_class_weight`, and all features unchanged.
- hypothesis: ExtraTrees may reduce RF-specific split variance through more randomized bagged trees and raise the leakage-free worst-seed floor after direct RF leaf/depth tweaks regressed.
- guard result: pass (`57 passed`).
- metric and current best: 0.142395 vs current best 0.145631; clean-v2 best remains 0.145631.
- decision: discard; metric regressed by -0.003236, so reverted experiment commit `9241bb9` with `581dfb2`.
- learning: The ET candidate was much slower and still matched the recent RF parameter-regression floor, so avoid `et_depth8_800trees` in the current RF/weight_delta context; next prefer a cheaper single axis such as RF tree-count reduction or a coherent release-safe feature-family test rather than another expensive ensemble-family switch.
---
## [2026-04-27T23:55:19+0900] - iteration 15
- mutation: switch top-level `model.kind` and params from the kept RF configuration to the schema-supported `logreg_balanced_c075` candidate, leaving split/window, synchronized `positive_class_weight`, and all features unchanged.
- hypothesis: A smoother balanced linear classifier may reduce tree-ensemble seed variance and raise the leakage-free worst-seed floor after RF parameter tweaks and the ET family switch regressed.
- guard result: pass (`57 passed`).
- metric and current best: 0.174757 vs prior clean-v2 current best 0.145631; new clean-v2 best 0.174757.
- decision: keep; metric improved by +0.029126 over the prior valid clean-v2 best.
- learning: The schema-supported LogReg family substantially improves the worst-seed floor despite repeated `lbfgs` convergence warnings and long runtime; next avoid returning to RF/ET micro-tweaks unless a new kept context demands it, and prefer LogReg-specific convergence/scaling/regularization experiments or release-safe feature additions that preserve this linear-family context.
---
## [2026-04-28T01:18:14+0900] - iteration 16
- mutation: lower active LogReg `C` from 0.75 to 0.50, leaving model family, `max_iter`, split/window, synchronized `positive_class_weight`, and features unchanged.
- hypothesis: Stronger L2 regularization in the kept LogReg context may reduce seed-level overfit and raise the leakage-free worst-seed holdout floor without changing release-safe feature inputs.
- guard result: pass (`57 passed`).
- metric and current best: 0.174757 vs current best 0.174757; clean-v2 best remains 0.174757.
- decision: discard; metric tied rather than strictly improving, so reverted experiment commit `f854243` with `f8cc798`.
- learning: `C=0.50` does not lift the LogReg clean-v2 floor and still emits repeated `lbfgs` convergence warnings, so avoid this stronger-regularization value in the current context; next prefer a different LogReg axis such as class weighting or a coherent release-safe feature-family test rather than another small downward `C` tweak.
---
## [2026-04-28T02:41:14+0900] - iteration 17
- mutation: raise active LogReg `max_iter` from 2000 to 5000, leaving model family, `C`, split/window, synchronized `positive_class_weight`, and features unchanged.
- hypothesis: More LogReg optimization budget may reduce `lbfgs` under-convergence in the kept linear context and improve leakage-free worst-seed holdout stability without changing release-safe feature inputs.
- guard result: pass (`57 passed`).
- metric and current best: 0.148867 vs current best 0.174757; clean-v2 best remains 0.174757.
- decision: discard; metric regressed by -0.025890, so reverted experiment commit `74792e4` with `a4d94f8`.
- learning: Increasing LogReg `max_iter` to 5000 was much slower, still emitted repeated `lbfgs` convergence warnings, and worsened the worst-seed floor; avoid further pure iteration-budget increases and next prefer a different single axis such as feature scaling support only if schema-supported, class weighting in the kept LogReg context, or a coherent release-safe feature-family test.
---
