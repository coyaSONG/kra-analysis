$autoresearch
Goal: Improve leakage-free recent-holdout 10-seed lowest hit rate for unordered top-3 horse prediction. Current best must be read from `autoresearch-results-clean-v2.tsv`.
Scope: packages/scripts/autoresearch/clean_model_config.json
Metric: lowest_overall_holdout_hit_rate
Direction: higher is better
Verify: sh packages/scripts/autoresearch/run_autoresearch_verify.sh
Guard: sh packages/scripts/autoresearch/run_autoresearch_guard.sh
Iterations: 1
Invalidation guard: `AUTORESEARCH_INVALIDATION.md` marks historical `autoresearch-results.tsv` rows invalid. Do not use historical rows for model selection. Continue only from `autoresearch-results-clean-v2.tsv` and the `full_year_2025_prerace_canonical_v2` dataset.
Operational guard: preserve the T-30 operational release contract. Any feature experiment must remain compatible with `data/contracts/t30_operational_release_features_v1.csv`; odds and timing-unverified sources are audit/backfill-only, not model inputs.

Continuation rules:
- If `autoresearch-results-clean-v2.tsv` already exists, do not reinitialize baseline.
- Complete exactly one additional resolved iteration.
- Use the next display iteration number as `max(numeric iteration column) + 1`.
- The supervisor may count physical TSV rows instead; do not try to reconcile historical duplicate rows during the iteration.
- A resolved iteration means:
  1. complete the research review checklist below,
  2. apply one small config mutation,
  3. run guard,
  4. run verify to completion,
  5. decide keep or discard,
  6. append one row to `autoresearch-results-clean-v2.tsv`,
  7. append one block to `progress.txt`,
  8. leave git working tree clean.
- Do not stop while verify is still running.
- If verify or guard fails, record a crash/discard row, clean up, and still finish the iteration.
- Do not ask the user questions.
- Exit only after the new iteration row is written and `git status --short` is clean.

Research review checklist:
- Read `AUTORESEARCH_RUNBOOK.md`, `progress.txt`, the last 20 rows of `autoresearch-results-clean-v2.tsv`, `git log --oneline -20`, and the current `clean_model_config.json`.
- Identify the current best metric from rows with `status=keep` or `status=baseline`.
- Identify the last 10 attempted experiment axes and mark them as `success`, `same`, `regression`, or `crash`.
- If the last 5 resolved iterations did not beat the best metric, choose a new research axis instead of a tiny value tweak.
- If the last 10 resolved iterations did not beat the best metric, choose a radical but still single-axis experiment.
- Do not repeat an axis/value pair already discarded unless the hypothesis explicitly combines it with a different kept context.

Allowed research axes, in priority order after plateau:
- model family switch within schema-supported candidates, if changing `model.kind` and params is valid.
- feature family changes that test a coherent hypothesis, not one arbitrary column at a time.
- class weighting or common hyperparameter changes, keeping duplicated schema fields synchronized.
- split/window changes only if they preserve recent-holdout, leakage-free, all-KRA evaluation semantics.
- HGB local parameter tweaks only when they directly exploit a recent keep.

Hypothesis requirement:
- Before editing, write a one-sentence hypothesis in your internal reasoning and use it to choose the mutation.
- The mutation description in `autoresearch-results-clean-v2.tsv` must state the research axis and why it was tested.
- `progress.txt` must append:
  - iteration number
  - mutation
  - hypothesis
  - guard result
  - metric and current best
  - decision
  - learning: what this result says to avoid or try next

Hard constraints:
- Experiment mutation may only edit `packages/scripts/autoresearch/clean_model_config.json`.
- Logging may edit only `autoresearch-results-clean-v2.tsv` and `progress.txt`.
- Do not edit verify, guard, dataset, runner, schema, or source data files.
- Do not use post-race, result, odds, payout, or market-derived fields.
- Do not add features outside the T-30 release contract unless they are already proven release-safe by the contract tests.
- Keep `model.positive_class_weight` synchronized with `experiment.common_hyperparameters.positive_class_weight`.
- Keep changes atomic: one research axis per iteration.
