# FastAPI API server environment example

# Core
ENVIRONMENT=development
PORT=8001
SECRET_KEY=changeme-secret-key

# Database (async SQLAlchemy URL)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/kra
# Disable metadata.create_all on startup outside local development.
DB_AUTO_CREATE_ON_STARTUP=false

# Redis / Cache
REDIS_URL=redis://localhost:6379/0

# External services (add as needed)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# Logging
LOG_LEVEL=info

# API Keys (Authentication)
#
# VALID_API_KEYS는 서버가 신뢰하는 API 키 목록입니다.
# JSON 배열(권장) 또는 콤마 구분 문자열 중 하나로 설정하세요.
#
# 예시 1) JSON 배열 (권장)
# VALID_API_KEYS=["prod-key-1","prod-key-2"]
#
# 예시 2) 콤마 구분 문자열
# VALID_API_KEYS=prod-key-1,prod-key-2
#
# 개발 모드 샘플 키 (운영 금지)
# VALID_API_KEYS=["test-api-key-123456789"]
