{
  "name": "@apps/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd ../../apps/api && PORT=8000 uv run uvicorn main_v2:app --reload --port 8000",
    "test": "uv run pytest -q || python -m pytest -q"
  }
}
