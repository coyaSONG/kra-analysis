# Reduce build context size for faster, reproducible builds

# VCS
.git
.gitignore

# Node
node_modules
npm-debug.log*
yarn.lock

# Build artifacts
dist
.tsbuildinfo
.turbo

# Coverage / test outputs
coverage
.nyc_output

# Local env and caches
.env
.env.*
cache
logs
tmp

# Examples, docs (not needed in runtime image)
docs
examples
data

# Tests are not required for production images
tests
