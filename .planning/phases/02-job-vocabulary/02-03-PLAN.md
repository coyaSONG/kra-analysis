---
phase: 02-job-vocabulary
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/services/kra_collection_module.py
  - apps/api/tasks/async_tasks.py
  - apps/api/services/job_service.py
  - apps/api/tests/unit/test_async_tasks.py
  - apps/api/tests/unit/test_job_service.py
  - apps/api/tests/integration/test_api_endpoints.py
autonomous: true
requirements:
  - JOBS-01
  - JOBS-02
must_haves:
  truths:
    - "One `/api/v2/collection/async` request produces one externally visible `batch` job."
    - "Async task updates and service statistics do not reintroduce internal alias names or legacy lifecycle values into persisted/public evidence."
    - "Following the collection receipt to `/api/v2/jobs/{id}` shows the same canonical vocabulary locked by Plans 01 and 02."
  artifacts:
    - path: apps/api/services/kra_collection_module.py
      provides: async collection receipt semantics that create a canonical `batch` job
    - path: apps/api/tasks/async_tasks.py
      provides: background task writes that keep persisted statuses on canonical public vocabulary
    - path: apps/api/tests/unit/test_async_tasks.py
      provides: regression coverage for batch/collection/full-pipeline task writes without public alias leakage
    - path: apps/api/tests/integration/test_api_endpoints.py
      provides: end-to-end receipt-to-job-detail canonical vocabulary proof
  key_links:
    - from: apps/api/services/kra_collection_module.py
      to: apps/api/services/job_service.py
      via: create_job(...) and start_job(...)
      pattern: create_job|start_job
    - from: apps/api/tasks/async_tasks.py
      to: apps/api/services/job_contract.py
      via: apply_job_shadow_fields(...)
      pattern: apply_job_shadow_fields
---

<objective>
Close the Phase 2 loop on async collection and runtime write paths so batch submission and follow-up job reads stay truthful end-to-end.

Purpose: Prove JOBS-01 and JOBS-02 under the real async collection entrypoint and clean up remaining internal vocabulary drift in runtime helpers.
Output: canonical async collection job behavior, task-write regressions, and service statistics/tests aligned to the public job vocabulary.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-job-vocabulary/02-CONTEXT.md
@.planning/phases/02-job-vocabulary/02-RESEARCH.md
@apps/api/routers/collection_v2.py
@apps/api/services/kra_collection_module.py
@apps/api/tasks/async_tasks.py
@apps/api/services/job_service.py
@apps/api/tests/unit/test_async_tasks.py
@apps/api/tests/integration/test_api_endpoints.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock async collection submission to the canonical `batch` job semantics</name>
  <files>apps/api/services/kra_collection_module.py, apps/api/tests/integration/test_api_endpoints.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/routers/collection_v2.py
    apps/api/services/kra_collection_module.py
    apps/api/tests/integration/test_api_endpoints.py
  </read_first>
  <action>Keep the current submission receipt transport (`status=accepted`, webhook URL, estimated time) but add or update integration coverage so the receipt's `job_id` resolves to a jobs detail payload whose `job.type` is `batch` and whose lifecycle values stay inside the canonical set. Do not add parent/child job exposure, and do not widen the collection request schema with explicit internal dispatch names.</action>
  <acceptance_criteria>
    - `rg -n '\"type\".*\"batch\"|job\\]\\[\"type\"\\] == \"batch\"' apps/api/tests/integration/test_api_endpoints.py` finds receipt-follow-up assertions.
    - `rg -n 'parent|child|batch_collect' apps/api/tests/integration/test_api_endpoints.py` returns no matches in the async collection endpoint assertions.
    - `cd apps/api && uv run pytest -q tests/integration/test_api_endpoints.py -o addopts='' -k 'async_collect or get_job_detail'` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/integration/test_api_endpoints.py -o addopts='' -k 'async_collect or get_job_detail'</automated>
  </verify>
  <done>The real async collection flow proves one externally visible `batch` job without leaking parent/child or internal dispatch vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up async task writes and service statistics</name>
  <files>apps/api/tasks/async_tasks.py, apps/api/services/job_service.py, apps/api/tests/unit/test_async_tasks.py, apps/api/tests/unit/test_job_service.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/tasks/async_tasks.py
    apps/api/services/job_service.py
    apps/api/tests/unit/test_async_tasks.py
    apps/api/tests/unit/test_job_service.py
  </read_first>
  <action>Rewrite async task and statistics expectations so persisted job evidence stays on public type/status vocabulary. For batch jobs, remove assertions that `job_kind_v2 == "batch_collect"` and make service statistics count `batch` / `collection` / other public types rather than internal dispatch labels. Keep internal task functions (`batch_collect`, `full_pipeline`) as implementation details, and do not expand Phase 2 into runner-boundary redesign.</action>
  <acceptance_criteria>
    - `rg -n 'job_kind_v2 == \"batch_collect\"|type_counts\\[\"batch_collect\"\\]|type_counts\\[\"collect_race\"\\]' apps/api/tests/unit/test_async_tasks.py apps/api/services/job_service.py apps/api/tests/unit/test_job_service.py` returns no matches.
    - `cd apps/api && uv run pytest -q tests/unit/test_async_tasks.py tests/unit/test_job_service.py -o addopts='' -k 'batch_collect or statistics or full_pipeline'` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_async_tasks.py tests/unit/test_job_service.py -o addopts='' -k 'batch_collect or statistics or full_pipeline'</automated>
  </verify>
  <done>Async task writes and service statistics stay consistent with the public vocabulary while internal task function names remain implementation detail only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collection submission -> jobs API follow-up | The async receipt and the later job detail must describe the same job semantics. |
| background task runner -> persisted job row | Internal task function names must not leak into persisted/public evidence through update helpers or stats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | `apps/api/services/kra_collection_module.py` receipt semantics | mitigate | Prove one-request-one-batch-job semantics via the real `/collection/async` to `/jobs/{id}` path. |
| T-02-08 | T | `apps/api/tasks/async_tasks.py` write helpers | mitigate | Keep persisted status/type evidence on canonical vocabulary regardless of internal task function names. |
| T-02-09 | R | `apps/api/services/job_service.py` statistics | mitigate | Count public job types in stats/tests so contributor-facing evidence no longer requires alias translation. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/unit/test_async_tasks.py tests/unit/test_job_service.py tests/integration/test_api_endpoints.py -o addopts='' -k 'async_collect or batch_collect or get_job_detail or statistics'`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Async collection submission and follow-up job reads remain aligned to the canonical Phase 2 job vocabulary
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-vocabulary/02-job-vocabulary-03-SUMMARY.md`
</output>
