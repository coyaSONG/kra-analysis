---
phase: 02-job-vocabulary
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/migrations/006_canonical_job_status_backfill.sql
  - apps/api/infrastructure/migration_manifest.py
  - apps/api/services/job_service.py
  - apps/api/routers/jobs_v2.py
  - apps/api/tests/unit/test_job_service.py
  - apps/api/tests/integration/test_jobs_v2_router_additional.py
  - apps/api/tests/integration/test_api_endpoints.py
autonomous: true
requirements:
  - JOBS-02
must_haves:
  truths:
    - "Jobs list/detail/cancel surfaces expose only canonical lifecycle values."
    - "Canonical-only reads do not depend on `lifecycle_state_v2` fallback logic."
    - "Legacy `running` and `retrying` rows are normalized before canonical-only reads ship."
  artifacts:
    - path: apps/api/migrations/006_canonical_job_status_backfill.sql
      provides: one-time data normalization for legacy job lifecycle values
    - path: apps/api/services/job_service.py
      provides: canonical-only jobs read/filter/cancel logic
    - path: apps/api/routers/jobs_v2.py
      provides: public query parsing and serialization limited to canonical status vocabulary
    - path: apps/api/tests/integration/test_jobs_v2_router_additional.py
      provides: regression coverage for canonical list/detail/cancel behavior without legacy read-compat
  key_links:
    - from: apps/api/infrastructure/migration_manifest.py
      to: apps/api/migrations/006_canonical_job_status_backfill.sql
      via: ACTIVE_MIGRATIONS
      pattern: ACTIVE_MIGRATIONS
    - from: apps/api/routers/jobs_v2.py
      to: apps/api/services/job_service.py
      via: list_jobs_with_total and cancel_job
      pattern: list_jobs_with_total|cancel_job
---

<objective>
Cut the jobs API over to canonical-only lifecycle reads and remove public legacy status compatibility.

Purpose: Implement the JOBS-02 retrieval/cancellation contract truthfully after the public enum core is locked.
Output: canonical-only jobs read path, migration-backed lifecycle normalization, and integration tests that reject legacy public status behavior.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-job-vocabulary/02-CONTEXT.md
@.planning/phases/02-job-vocabulary/02-RESEARCH.md
@apps/api/infrastructure/migration_manifest.py
@apps/api/services/job_service.py
@apps/api/routers/jobs_v2.py
@apps/api/tests/integration/test_jobs_v2_router_additional.py
@apps/api/tests/integration/test_api_endpoints.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add migration-backed lifecycle normalization for legacy rows</name>
  <files>apps/api/migrations/006_canonical_job_status_backfill.sql, apps/api/infrastructure/migration_manifest.py, apps/api/tests/unit/test_job_service.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/infrastructure/migration_manifest.py
    apps/api/scripts/apply_migrations.py
    apps/api/models/database_models.py
    apps/api/tests/unit/test_job_service.py
  </read_first>
  <action>Create a focused manifest-tracked SQL migration that rewrites `jobs.status IN ('running','retrying')` to `processing` and aligns any compatibility lifecycle column to the same canonical value where necessary. Update the migration manifest to include the new file. Rewrite unit tests so they model the post-migration state instead of relying on legacy rows surviving the canonical-only cutover. Do not attempt PostgreSQL enum-label deletion in this plan; data truth is the objective.</action>
  <acceptance_criteria>
    - `rg -n 'running|retrying' apps/api/migrations/006_canonical_job_status_backfill.sql` shows only rewrite source values, not target contract values in code comments or tests.
    - `rg -n '006_canonical_job_status_backfill.sql' apps/api/infrastructure/migration_manifest.py` finds the new manifest entry.
    - `cd apps/api && uv run pytest -q tests/unit/test_job_service.py -o addopts=''` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_job_service.py -o addopts=''</automated>
  </verify>
  <done>A manifest-tracked migration exists for legacy lifecycle rows, and service tests no longer rely on pre-cutover `running` or `retrying` behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Remove jobs API legacy read compatibility</name>
  <files>apps/api/services/job_service.py, apps/api/routers/jobs_v2.py, apps/api/tests/integration/test_jobs_v2_router_additional.py, apps/api/tests/integration/test_api_endpoints.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/services/job_service.py
    apps/api/routers/jobs_v2.py
    apps/api/tests/integration/test_jobs_v2_router_additional.py
    apps/api/tests/integration/test_api_endpoints.py
  </read_first>
  <action>Delete the `lifecycle_state_v2` dual-read filter and `running -> processing` request compatibility from `list_jobs_with_total()` and related router parsing. Make list/detail/cancel integration tests assert canonical statuses only, including `processing` for active jobs and `cancelled` for cancelled jobs. Keep ownership/auth behavior unchanged, and do not introduce new routes or response fields.</action>
  <acceptance_criteria>
    - `rg -n 'lifecycle_state_v2|legacy_status_values|running' apps/api/services/job_service.py apps/api/routers/jobs_v2.py` returns no matches for public read/filter compatibility logic.
    - `rg -n 'processing_filter_matches_legacy_running_row|normalizes_running_status' apps/api/tests/integration/test_jobs_v2_router_additional.py apps/api/tests/unit/test_job_service.py` returns no matches.
    - `cd apps/api && uv run pytest -q tests/integration/test_jobs_v2_router_additional.py tests/integration/test_api_endpoints.py -o addopts='' -k 'jobs or cancel_job or get_job_detail'` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/integration/test_jobs_v2_router_additional.py tests/integration/test_api_endpoints.py -o addopts='' -k 'jobs or cancel_job or get_job_detail'</automated>
  </verify>
  <done>Jobs list/detail/cancel paths operate on canonical lifecycle values only, and public tests no longer encode legacy `running` compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing jobs table -> new read path | Legacy rows can break canonical-only reads unless normalized first. |
| router query params -> service filters | Public filters must not silently accept removed lifecycle vocabulary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | D | `apps/api/services/job_service.py` read filters | mitigate | Normalize stored data first, then delete `lifecycle_state_v2` fallback logic. |
| T-02-05 | I | `apps/api/routers/jobs_v2.py` query parsing | mitigate | Restrict router enums to canonical statuses so OpenAPI and runtime parsing stay truthful. |
| T-02-06 | R | legacy integration tests | mitigate | Replace tests that bless `running` compatibility with canonical list/detail/cancel assertions. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/unit/test_job_service.py tests/integration/test_jobs_v2_router_additional.py tests/integration/test_api_endpoints.py -o addopts='' -k 'jobs or cancel_job or get_job_detail or list_jobs'`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Jobs API list/detail/cancel paths are canonical-only after migration-backed data normalization
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-vocabulary/02-job-vocabulary-02-SUMMARY.md`
</output>
