---
phase: 02-job-vocabulary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/models/job_dto.py
  - apps/api/models/database_models.py
  - apps/api/services/job_contract.py
  - apps/api/services/job_service.py
  - apps/api/tests/unit/test_job_contract.py
  - apps/api/tests/unit/test_job_dispatch.py
  - apps/api/tests/unit/test_job_service.py
autonomous: true
requirements:
  - JOBS-01
must_haves:
  truths:
    - "Persisted and public job types stay on product/API vocabulary such as `collection` and `batch`."
    - "Internal dispatch still reaches `collect_race` and `batch_collect` without leaking those names into canonical persisted fields."
    - "Public lifecycle enums no longer advertise `running` or `retrying`."
  artifacts:
    - path: apps/api/services/job_contract.py
      provides: one explicit separation between public job vocabulary and internal dispatch aliases
    - path: apps/api/models/job_dto.py
      provides: public DTO enums limited to canonical type/status values
    - path: apps/api/models/database_models.py
      provides: ORM enum definitions that align with the public job vocabulary
    - path: apps/api/tests/unit/test_job_contract.py
      provides: regression coverage proving internal aliases are dispatch-only and persisted fields stay canonical
  key_links:
    - from: apps/api/services/job_service.py
      to: apps/api/services/job_contract.py
      via: normalize_dispatch_action(...)
      pattern: normalize_dispatch_action
    - from: apps/api/models/job_dto.py
      to: apps/api/routers/jobs_v2.py
      via: JobStatus and JobType query/response enums
      pattern: JobStatus|JobType
---

<objective>
Lock the public job vocabulary at the model and service core so `jobs.type` / `jobs.status` remain canonical while internal dispatch aliases stay internal.

Purpose: Implement the JOBS-01 source-of-truth boundary before Phase 2 removes compatibility reads.
Output: canonical DTO/ORM enums, clarified job-contract helpers, and unit tests that stop treating `batch_collect` or `running` as public truth.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-job-vocabulary/02-CONTEXT.md
@.planning/phases/02-job-vocabulary/02-RESEARCH.md
@.planning/codebase/CONCERNS.md
@docs/plans/2026-03-19-architecture-remediation-execplan.md
@apps/api/models/job_dto.py
@apps/api/models/database_models.py
@apps/api/services/job_contract.py
@apps/api/services/job_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the public enum and normalization contract</name>
  <files>apps/api/models/job_dto.py, apps/api/models/database_models.py, apps/api/services/job_contract.py, apps/api/tests/unit/test_job_contract.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/models/job_dto.py
    apps/api/models/database_models.py
    apps/api/services/job_contract.py
    apps/api/tests/unit/test_job_contract.py
  </read_first>
  <action>Change the public enum contract so DTO and ORM `JobStatus` only contain `pending`, `queued`, `processing`, `completed`, `failed`, and `cancelled`. Keep `normalize_dispatch_action()` able to translate public types like `collection` and `batch` to internal dispatch actions, but stop treating internal names as canonical persisted job kinds. Update `normalize_job_kind()` and `apply_job_shadow_fields()` so public rows mirror public vocabulary (`batch`, not `batch_collect`; `processing`, not `running`) whenever compatibility fields are still written.</action>
  <acceptance_criteria>
    - `rg -n 'RUNNING|RETRYING' apps/api/models/job_dto.py apps/api/models/database_models.py` returns no matches.
    - `rg -n 'batch_collect' apps/api/tests/unit/test_job_contract.py` shows internal alias usage only in dispatch assertions, not persisted field expectations.
    - `cd apps/api && uv run pytest -q tests/unit/test_job_contract.py -o addopts=''` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_job_contract.py -o addopts=''</automated>
  </verify>
  <done>Public job enums exclude `running` and `retrying`, and contract tests prove internal dispatch aliases no longer masquerade as canonical persisted values.</done>
</task>

<task type="auto">
  <name>Task 2: Align JobService with the canonical public vocabulary</name>
  <files>apps/api/services/job_service.py, apps/api/tests/unit/test_job_dispatch.py, apps/api/tests/unit/test_job_service.py</files>
  <read_first>
    .planning/phases/02-job-vocabulary/02-CONTEXT.md
    .planning/phases/02-job-vocabulary/02-RESEARCH.md
    apps/api/services/job_service.py
    apps/api/tests/unit/test_job_dispatch.py
    apps/api/tests/unit/test_job_service.py
  </read_first>
  <action>Make `JobService.create_job()` and `JobService._dispatch_task()` treat `job.type` as the canonical persisted/public field while using dispatch translation only at execution time. Keep support for internal-only dispatch inputs where needed for compatibility tests, but rewrite unit tests so public job creation and persisted field assertions use `collection` / `batch` rather than `collect_race` / `batch_collect`. Do not change runner architecture or add new public job types in this plan.</action>
  <acceptance_criteria>
    - `rg -n 'type=\"collect_race\"|type=\"batch_collect\"' apps/api/tests/unit/test_job_service.py apps/api/tests/unit/test_job_dispatch.py` returns no matches in public-persistence tests.
    - `rg -n 'job_kind_v2 == \"batch_collect\"|lifecycle_state_v2 == \"processing\".*running' apps/api/tests/unit/test_job_service.py` returns no matches.
    - `cd apps/api && uv run pytest -q tests/unit/test_job_dispatch.py tests/unit/test_job_service.py -o addopts=''` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_job_dispatch.py tests/unit/test_job_service.py -o addopts=''</automated>
  </verify>
  <done>JobService persists and reasons about public job vocabulary while dispatch translation remains isolated inside the service seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/DTO -> ORM row | Client-visible type/status vocabulary must match what the database row records for the same job. |
| ORM row -> dispatch service | Service may need internal action names, but those names must not leak back into canonical persisted/public fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `apps/api/services/job_contract.py` | mitigate | Separate public vocabulary helpers from dispatch alias translation and stop persisting internal alias names as canonical evidence. |
| T-02-02 | I | DTO/ORM enums | mitigate | Remove `running` / `retrying` from public enums so generated docs and filter parsing cannot expose stale lifecycle vocabulary. |
| T-02-03 | R | unit tests | mitigate | Rewrite unit tests that currently bless `batch_collect` or `running` as public truth. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/unit/test_job_contract.py tests/unit/test_job_dispatch.py tests/unit/test_job_service.py -o addopts=''`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Public job enums and persisted field assertions are canonical-only
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-vocabulary/02-job-vocabulary-01-SUMMARY.md`
</output>
