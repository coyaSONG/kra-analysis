---
phase: 03-unified-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/infrastructure/migration_manifest.py
  - apps/api/scripts/apply_migrations.py
  - apps/api/tests/unit/test_migration_manifest.py
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "Contributor can point to one manifest file that names the active unified migration chain and the legacy baseline that must stay inactive."
    - "The migration runner reads the same active chain and head that startup verification will enforce."
    - "Legacy migration files are visible as inactive artifacts, not inferred as current truth from directory order."
  artifacts:
    - path: apps/api/infrastructure/migration_manifest.py
      provides: canonical active-chain and legacy-conflict metadata
    - path: apps/api/scripts/apply_migrations.py
      provides: manifest-driven migration file validation and operator-facing status output
    - path: apps/api/tests/unit/test_migration_manifest.py
      provides: regression coverage for active-head and inactive-legacy manifest truth
  key_links:
    - from: apps/api/infrastructure/migration_manifest.py
      to: apps/api/scripts/apply_migrations.py
      via: get_active_migration_names/get_active_migration_paths/get_required_migration_head
      pattern: get_active_migration_|get_required_migration_head
    - from: apps/api/infrastructure/migration_manifest.py
      to: apps/api/tests/unit/test_migration_manifest.py
      via: direct import assertions
      pattern: ACTIVE_MIGRATIONS|legacy
---

<objective>
Lock the unified migration manifest in code as the only schema source of truth before any startup or proof work builds on it.

Purpose: Phase 3 must fail closed on mixed legacy/unified state, which requires one explicit manifest contract instead of directory-order assumptions.
Output: manifest metadata for active vs inactive migration files, manifest-driven runner wiring, and unit coverage for source-of-truth behavior.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-bootstrap/03-RESEARCH.md
@.planning/phases/03-unified-bootstrap/03-VALIDATION.md
@apps/api/infrastructure/migration_manifest.py
@apps/api/scripts/apply_migrations.py

<interfaces>
From `apps/api/infrastructure/migration_manifest.py`:
```python
ACTIVE_MIGRATIONS = [
    "001_unified_schema.sql",
    "002_add_prediction_created_by.sql",
    "003_add_race_odds.sql",
    "004_add_job_shadow_fields.sql",
    "005_add_usage_events.sql",
    "006_canonical_job_status_backfill.sql",
]

def get_active_migration_names() -> list[str]: ...
def get_active_migration_paths() -> list[Path]: ...
def get_required_migration_head() -> str | None: ...
```

From `apps/api/scripts/apply_migrations.py`:
```python
def validate_manifest_files() -> list[Path]: ...
async def check_migration_status(conn): ...
async def verify_schema(conn): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify active vs legacy migration truth in the manifest</name>
  <files>apps/api/infrastructure/migration_manifest.py, apps/api/tests/unit/test_migration_manifest.py</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    apps/api/infrastructure/migration_manifest.py
    apps/api/migrations/001_initial_schema.sql
    apps/api/migrations/001_unified_schema.sql
    apps/api/tests/unit/test_database_migration_guard.py
    apps/api/tests/unit/test_migration_manifest.py
  </read_first>
  <action>Add explicit exported metadata in `apps/api/infrastructure/migration_manifest.py` for the active unified chain and the inactive legacy baseline. Keep `001_initial_schema.sql` out of `ACTIVE_MIGRATIONS`, add helper(s) that expose legacy-only conflict markers needed for later mixed-state rejection, and add `apps/api/tests/unit/test_migration_manifest.py` proving the active chain starts at `001_unified_schema.sql`, ends at `006_canonical_job_status_backfill.sql`, and never promotes the legacy baseline. Do not infer active truth from filesystem sort order.</action>
  <acceptance_criteria>
    - `rg -n '001_initial_schema.sql' apps/api/infrastructure/migration_manifest.py apps/api/tests/unit/test_migration_manifest.py` shows the legacy baseline only in inactive/legacy metadata or tests, never inside `ACTIVE_MIGRATIONS`.
    - `rg -n '006_canonical_job_status_backfill.sql' apps/api/infrastructure/migration_manifest.py apps/api/tests/unit/test_migration_manifest.py` confirms the required active head is still the canonical job-status backfill migration.
    - `cd apps/api && uv run pytest -q tests/unit/test_migration_manifest.py -o addopts=''` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_migration_manifest.py -o addopts=''</automated>
  </verify>
  <done>The manifest file publishes one active unified chain plus explicit inactive legacy markers, and the new unit test locks that truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the migration runner consume the canonical manifest contract</name>
  <files>apps/api/scripts/apply_migrations.py, apps/api/tests/unit/test_migration_manifest.py</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    apps/api/infrastructure/migration_manifest.py
    apps/api/scripts/apply_migrations.py
    apps/api/tests/unit/test_migration_manifest.py
    apps/api/README.md
  </read_first>
  <action>Update `apps/api/scripts/apply_migrations.py` so `validate_manifest_files()` and CLI status output read the active/inactive manifest helpers instead of treating the migrations directory as implicit truth. The script must print the active head name, list inactive legacy files separately, and fail with a non-zero error when any required active file is missing. Extend `apps/api/tests/unit/test_migration_manifest.py` or adjacent focused tests to cover the runner-side manifest validation behavior. Do not auto-apply inactive legacy files and do not add fallback logic that silently heals mixed state.</action>
  <acceptance_criteria>
    - `rg -n 'inactive|legacy|required head|active head' apps/api/scripts/apply_migrations.py` finds explicit runner messaging tied to manifest truth.
    - `rg -n 'validate_manifest_files' apps/api/scripts/apply_migrations.py apps/api/tests/unit/test_migration_manifest.py` confirms the script validation path is exercised by tests.
    - `cd apps/api && uv run pytest -q tests/unit/test_migration_manifest.py -o addopts=''` passes after the runner wiring change.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_migration_manifest.py -o addopts=''</automated>
  </verify>
  <done>The migration runner now consumes the same manifest truth the startup guard will enforce, and unit coverage locks the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrations directory -> manifest contract | Extra legacy files in the tree can be mistaken for active schema truth if the manifest is ambiguous. |
| manifest contract -> operator runner | If the runner does not consume manifest truth directly, operators can bootstrap the wrong baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `apps/api/infrastructure/migration_manifest.py` | mitigate | Encode the active unified chain and inactive legacy markers explicitly so directory contents cannot redefine schema truth. |
| T-03-02 | D | `apps/api/scripts/apply_migrations.py` | mitigate | Make validation fail when required manifest files are missing and keep inactive legacy files out of the apply path. |
| T-03-03 | R | manifest regression tests | mitigate | Add unit coverage that proves the active head and inactive legacy baseline remain stable. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/unit/test_migration_manifest.py -o addopts=''`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has one explicit migration manifest truth for active vs inactive files
- The migration runner reads that truth directly instead of inferring from file ordering
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-bootstrap/03-unified-bootstrap-01-SUMMARY.md`
</output>
