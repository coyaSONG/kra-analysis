---
phase: 03-unified-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - apps/api/tests/platform/fixtures.py
  - apps/api/tests/integration/test_bootstrap_manifest.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "Operator can prove a fresh database bootstraps from the unified manifest without production-path `create_all()` fallback."
    - "The test harness has a dedicated migration-first bootstrap path instead of using SQLite `create_all()` as evidence for production bootstrap."
    - "CI executes a non-test bootstrap proof so schema truth is checked continuously."
  artifacts:
    - path: apps/api/tests/platform/fixtures.py
      provides: bootstrap-proof fixture path for disposable Postgres databases
    - path: apps/api/tests/integration/test_bootstrap_manifest.py
      provides: fresh-database migration-first bootstrap proof
    - path: .github/workflows/ci.yml
      provides: automated CI lane for non-test manifest bootstrap verification
  key_links:
    - from: apps/api/tests/platform/fixtures.py
      to: apps/api/scripts/apply_migrations.py
      via: bootstrap proof fixture or helper invocation
      pattern: apply_migrations|schema_migrations
    - from: .github/workflows/ci.yml
      to: apps/api/tests/integration/test_bootstrap_manifest.py
      via: dedicated bootstrap verification step
      pattern: bootstrap|migration
---

<objective>
Add a fresh-database proof harness that demonstrates the unified migration chain is sufficient for bootstrap, then enforce that proof in CI with a non-test environment.

Purpose: SCHEMA-01 is not satisfied by unit-only guard logic; the repo needs an executable proof that empty Postgres bootstrap works without production-path `create_all()`.
Output: dedicated bootstrap fixtures/tests plus a CI lane that runs migration-first startup verification in non-test mode.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-bootstrap/03-RESEARCH.md
@.planning/phases/03-unified-bootstrap/03-VALIDATION.md
@.planning/phases/03-unified-bootstrap/03-02-PLAN.md
@apps/api/tests/platform/fixtures.py
@apps/api/scripts/apply_migrations.py
@apps/api/infrastructure/database.py
@.github/workflows/ci.yml

<interfaces>
From `apps/api/tests/platform/fixtures.py`:
```python
@pytest_asyncio.fixture(scope="function")
async def test_db_engine(test_settings): ...

@pytest_asyncio.fixture(scope="function")
async def db_session(test_db_engine): ...
```

From `.github/workflows/ci.yml`:
```yaml
- name: Unit tests
  env:
    ENVIRONMENT: test
- name: Integration tests
  env:
    ENVIRONMENT: test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a migration-first fresh-bootstrap proof harness</name>
  <files>apps/api/tests/platform/fixtures.py, apps/api/tests/integration/test_bootstrap_manifest.py</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    .planning/phases/03-unified-bootstrap/03-VALIDATION.md
    apps/api/tests/platform/fixtures.py
    apps/api/scripts/apply_migrations.py
    apps/api/infrastructure/database.py
    apps/api/tests/integration/test_bootstrap_manifest.py
    apps/api/tests/integration/test_api_endpoints.py
  </read_first>
  <action>Add a dedicated Postgres bootstrap-proof helper in `apps/api/tests/platform/fixtures.py` that prepares an isolated empty database for integration tests and runs the manifest-first migration path from `scripts/apply_migrations.py`. Create `apps/api/tests/integration/test_bootstrap_manifest.py` to prove: empty DB starts with no application tables, `scripts/apply_migrations.py` creates `schema_migrations` plus required unified tables, and non-test startup can initialize successfully afterward. Keep the existing SQLite `Base.metadata.create_all()` fixture for fast unit tests, but do not use it as evidence for the production bootstrap contract.</action>
  <acceptance_criteria>
    - `rg -n 'apply_migrations|schema_migrations|Base.metadata.create_all' apps/api/tests/platform/fixtures.py apps/api/tests/integration/test_bootstrap_manifest.py` shows a dedicated migration-first proof path and keeps `Base.metadata.create_all` confined to the fast test harness.
    - `cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py -o addopts=''` passes.
    - `cd apps/api && uv run pytest -q tests/platform tests/integration/test_bootstrap_manifest.py -o addopts='' -k 'bootstrap or migration'` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/platform tests/integration/test_bootstrap_manifest.py -o addopts='' -k 'bootstrap or migration'</automated>
  </verify>
  <done>The repo has an executable fresh-bootstrap proof that uses the unified migration chain, not production-path `create_all()`, as the evidence source.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce non-test bootstrap proof in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    .planning/phases/03-unified-bootstrap/03-VALIDATION.md
    .github/workflows/ci.yml
    apps/api/scripts/run_quality_ci.sh
    apps/api/tests/integration/test_bootstrap_manifest.py
    apps/api/tests/integration/test_startup_manifest_rejection.py
  </read_first>
  <action>Update `.github/workflows/ci.yml` with a dedicated bootstrap verification step that runs after dependencies install and before coverage upload. The step must use the CI postgres service, set `DATABASE_URL` to that service, set `ENVIRONMENT=development` or another non-test value, execute the fresh-bootstrap proof command for `tests/integration/test_bootstrap_manifest.py`, and run the startup rejection proof for `tests/integration/test_startup_manifest_rejection.py`. Keep existing unit/integration jobs intact; add a focused lane instead of broad CI refactoring.</action>
  <acceptance_criteria>
    - `rg -n 'bootstrap|test_bootstrap_manifest.py|test_startup_manifest_rejection.py|ENVIRONMENT: (development|staging|production)' .github/workflows/ci.yml` finds the dedicated non-test bootstrap verification step.
    - `rg -n 'ENVIRONMENT: test' .github/workflows/ci.yml` still finds the existing unit/integration steps, proving this plan did not repurpose the whole workflow.
    - `cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts=''` is the command encoded by the new workflow step.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts=''</automated>
  </verify>
  <done>CI now runs a non-test bootstrap proof against the unified manifest so fresh-database and startup rejection guarantees are continuously checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production bootstrap claim | SQLite `create_all()` can give false confidence if reused as proof for production bootstrap behavior. |
| CI postgres service -> bootstrap verification | If CI never runs non-test startup checks, manifest drift can land undetected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | R | `apps/api/tests/platform/fixtures.py` | mitigate | Add a dedicated manifest-first bootstrap fixture so proof coverage is separate from the fast SQLite harness. |
| T-03-08 | D | `.github/workflows/ci.yml` | mitigate | Add a non-test bootstrap verification step that exercises fresh DB migration and startup rejection against Postgres. |
| T-03-09 | T | `apps/api/tests/integration/test_bootstrap_manifest.py` | mitigate | Assert on `schema_migrations` and required unified tables so the proof fails if bootstrap uses the wrong baseline. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/platform tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts='' -k 'bootstrap or migration or startup'`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- A fresh Postgres bootstrap can be proven through the manifest-first path
- CI runs that proof in a non-test environment
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-bootstrap/03-unified-bootstrap-03-SUMMARY.md`
</output>
