---
phase: 03-unified-bootstrap
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - apps/api/scripts/setup.sh
  - apps/api/Makefile
  - apps/api/README.md
  - apps/api/docs/SUPABASE_SETUP.md
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "Operator sees one migration-first bootstrap command set across shell setup, Makefile, README, and Supabase setup docs."
    - "Operator-facing instructions no longer point at `001_initial_schema.sql`, Alembic, or inactive app entrypoints."
    - "Operator docs explain that mixed legacy/unified schema state must fail closed and be corrected via the manifest path."
  artifacts:
    - path: apps/api/scripts/setup.sh
      provides: setup-time migration-first bootstrap guidance
    - path: apps/api/Makefile
      provides: operator shortcuts aligned to `scripts/apply_migrations.py`
    - path: apps/api/README.md
      provides: concise runtime and migration-first operator entrypoint truth
    - path: apps/api/docs/SUPABASE_SETUP.md
      provides: detailed migration-first verification path for empty DB bootstrap
  key_links:
    - from: apps/api/scripts/setup.sh
      to: apps/api/scripts/apply_migrations.py
      via: next-step setup instructions
      pattern: apply_migrations.py
    - from: apps/api/README.md
      to: apps/api/docs/SUPABASE_SETUP.md
      via: operator setup references
      pattern: SUPABASE_SETUP
---

<objective>
Clean up the operator entrypoints so every bootstrap instruction points at the unified migration chain and fail-closed startup contract.

Purpose: Phase 3 is incomplete if code truth says one thing while setup scripts and operator docs still advertise legacy SQL or Alembic commands.
Output: migration-first setup script, Makefile, README, and focused Supabase setup guidance aligned to the Phase 3 bootstrap contract.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-bootstrap/03-RESEARCH.md
@.planning/phases/03-unified-bootstrap/03-03-PLAN.md
@apps/api/scripts/setup.sh
@apps/api/Makefile
@apps/api/README.md
@apps/api/docs/SUPABASE_SETUP.md
@apps/api/scripts/apply_migrations.py
@apps/api/main_v2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align shell and Makefile entrypoints to the manifest-first bootstrap path</name>
  <files>apps/api/scripts/setup.sh, apps/api/Makefile</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    apps/api/scripts/setup.sh
    apps/api/Makefile
    apps/api/scripts/apply_migrations.py
    apps/api/main_v2.py
    apps/api/.env.example
  </read_first>
  <action>Rewrite `apps/api/scripts/setup.sh` and `apps/api/Makefile` so operator-facing commands use the active runtime and migration path: Python 3.13 + `uv`, `uv run python3 scripts/apply_migrations.py`, and `uv run uvicorn main_v2:app`. Remove Alembic and `migrations/001_initial_schema.sql` directions, and add an explicit mixed-state warning that the app will fail closed until the manifest path is applied cleanly. Keep scope limited to bootstrap/operator commands; do not redesign container workflows or add new deployment tooling.</action>
  <acceptance_criteria>
    - `rg -n '001_initial_schema.sql|alembic upgrade head|api.main:app|Python 3.11' apps/api/scripts/setup.sh apps/api/Makefile` returns no matches.
    - `rg -n 'apply_migrations.py|main_v2:app|fail closed|mixed' apps/api/scripts/setup.sh apps/api/Makefile` finds the migration-first bootstrap guidance and mixed-state warning.
    - `bash -n apps/api/scripts/setup.sh` passes.
  </acceptance_criteria>
  <verify>
    <automated>bash -n apps/api/scripts/setup.sh</automated>
  </verify>
  <done>The shell entrypoints and Makefile no longer advertise legacy schema bootstrap commands or inactive app entrypoints.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten README and Supabase setup doc to one bootstrap verification path</name>
  <files>apps/api/README.md, apps/api/docs/SUPABASE_SETUP.md</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    apps/api/README.md
    apps/api/docs/SUPABASE_SETUP.md
    apps/api/scripts/apply_migrations.py
    .github/workflows/ci.yml
    apps/api/tests/integration/test_bootstrap_manifest.py
  </read_first>
  <action>Update only the Phase 3-relevant operator sections in `apps/api/README.md` and `apps/api/docs/SUPABASE_SETUP.md` so they describe one migration-first empty-database bootstrap sequence, one non-test startup verification command, and one fail-closed remediation path for mixed legacy/unified state. Remove stale wording that claims job vocabulary is still being unified or that operators should inspect inactive legacy baseline files as if they were current truth. Do not expand into broad architecture or later-phase documentation cleanup.</action>
  <acceptance_criteria>
    - `rg -n '001_initial_schema.sql|alembic|job vocabulary.*정리 대상|정리 작업이 진행 중' apps/api/README.md apps/api/docs/SUPABASE_SETUP.md` returns no matches in the updated operator sections.
    - `rg -n 'apply_migrations.py|test_bootstrap_manifest.py|test_startup_manifest_rejection.py|mixed legacy|fail closed' apps/api/README.md apps/api/docs/SUPABASE_SETUP.md` finds the canonical bootstrap and remediation path.
    - `cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts=''` remains the documented verification command set.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts=''</automated>
  </verify>
  <done>The operator-facing README and Supabase guide now describe the same manifest-first bootstrap and mixed-state remediation flow as the code and CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator docs/scripts -> production database actions | Stale commands can bootstrap the wrong baseline even if code is correct. |
| README/setup guidance -> runtime entrypoint | Incorrect startup commands can bypass or misrepresent the active runtime contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T | `apps/api/scripts/setup.sh` and `apps/api/Makefile` | mitigate | Replace legacy SQL and Alembic instructions with `scripts/apply_migrations.py` and the active `main_v2.py` runtime path. |
| T-03-11 | I | `apps/api/README.md` and `apps/api/docs/SUPABASE_SETUP.md` | mitigate | Document one migration-first bootstrap sequence plus explicit mixed-state fail-closed remediation. |
| T-03-12 | R | operator verification commands | mitigate | Reuse the concrete Phase 3 bootstrap/startup pytest commands so docs and CI describe the same proof path. |
</threat_model>

<verification>
Run `bash -n apps/api/scripts/setup.sh` and `cd apps/api && uv run pytest -q tests/integration/test_bootstrap_manifest.py tests/integration/test_startup_manifest_rejection.py -o addopts=''`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operator-facing bootstrap commands align with the manifest-first runtime contract
- No legacy bootstrap path remains in Phase 3 entrypoint docs or scripts
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-bootstrap/03-unified-bootstrap-04-SUMMARY.md`
</output>
