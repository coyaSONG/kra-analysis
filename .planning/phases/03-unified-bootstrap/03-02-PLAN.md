---
phase: 03-unified-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/api/infrastructure/database.py
  - apps/api/tests/unit/test_database_migration_guard.py
  - apps/api/tests/integration/test_startup_manifest_rejection.py
autonomous: true
requirements:
  - SCHEMA-02
must_haves:
  truths:
    - "App startup rejects databases whose `schema_migrations` state differs from the active manifest."
    - "App startup also rejects mixed legacy/unified physical schema state instead of silently continuing."
    - "Non-test lifespan startup propagates manifest guard failures before the app begins serving requests."
  artifacts:
    - path: apps/api/infrastructure/database.py
      provides: fail-closed manifest and mixed-state startup guard
    - path: apps/api/tests/unit/test_database_migration_guard.py
      provides: branch coverage for missing, unexpected, and mixed-state guard failures
    - path: apps/api/tests/integration/test_startup_manifest_rejection.py
      provides: non-test FastAPI lifespan failure coverage for startup guard errors
  key_links:
    - from: apps/api/infrastructure/database.py
      to: apps/api/infrastructure/migration_manifest.py
      via: active manifest names and legacy conflict markers
      pattern: get_active_migration_names|get_legacy_conflict
    - from: apps/api/main_v2.py
      to: apps/api/infrastructure/database.py
      via: lifespan -> init_db
      pattern: await init_db
---

<objective>
Harden startup verification so non-test environments reject missing, unexpected, or mixed legacy/unified schema state against the canonical manifest.

Purpose: SCHEMA-02 is the fail-closed contract for runtime safety; the app must stop before serving if schema truth is ambiguous.
Output: stricter database guard logic plus unit and lifespan-level startup rejection coverage.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-bootstrap/03-RESEARCH.md
@.planning/phases/03-unified-bootstrap/03-VALIDATION.md
@.planning/phases/03-unified-bootstrap/03-01-PLAN.md
@apps/api/infrastructure/database.py
@apps/api/main_v2.py
@apps/api/tests/unit/test_database_migration_guard.py

<interfaces>
From `apps/api/infrastructure/database.py`:
```python
async def get_applied_migrations() -> dict[str, str]: ...
async def require_migration_manifest() -> None: ...
async def init_db(): ...
```

From `apps/api/main_v2.py`:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on mixed legacy and manifest mismatch state</name>
  <files>apps/api/infrastructure/database.py, apps/api/tests/unit/test_database_migration_guard.py</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    .planning/phases/03-unified-bootstrap/03-01-PLAN.md
    apps/api/infrastructure/migration_manifest.py
    apps/api/infrastructure/database.py
    apps/api/tests/unit/test_database_migration_guard.py
    apps/api/migrations/001_initial_schema.sql
    apps/api/migrations/001_unified_schema.sql
  </read_first>
  <action>Extend `apps/api/infrastructure/database.py` so `require_migration_manifest()` checks both manifest rows and legacy-only physical markers from `migration_manifest.py`. Raise distinct `RuntimeError` messages for: missing/unreadable `schema_migrations`, missing manifest rows, unexpected migration names, and mixed legacy/unified table state. The mixed-state branch must fail closed and instruct operators to use `scripts/apply_migrations.py`; do not auto-heal by creating tables, dropping tables, or falling back to `Base.metadata.create_all()` in any non-test path.</action>
  <acceptance_criteria>
    - `rg -n 'mixed|legacy|schema_migrations table missing or unreadable|manifest mismatch' apps/api/infrastructure/database.py apps/api/tests/unit/test_database_migration_guard.py` finds explicit mixed-state and manifest-mismatch handling.
    - `rg -n 'create_all' apps/api/infrastructure/database.py` shows `Base.metadata.create_all` remains limited to the `settings.environment == \"test\"` or sqlite branch.
    - `cd apps/api && uv run pytest -q tests/unit/test_database_migration_guard.py -o addopts=''` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_database_migration_guard.py -o addopts=''</automated>
  </verify>
  <done>The startup guard rejects missing, unexpected, and mixed-state schema conditions in non-test environments without any silent repair path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove FastAPI lifespan startup stops on manifest guard failures</name>
  <files>apps/api/tests/integration/test_startup_manifest_rejection.py</files>
  <read_first>
    .planning/phases/03-unified-bootstrap/03-RESEARCH.md
    apps/api/main_v2.py
    apps/api/infrastructure/database.py
    apps/api/tests/platform/fixtures.py
    apps/api/tests/integration/test_startup_manifest_rejection.py
  </read_first>
  <action>Create `apps/api/tests/integration/test_startup_manifest_rejection.py` to exercise `create_app()`/lifespan startup in a non-test environment and assert that manifest guard failures propagate before request serving. Include at least one case for a mixed legacy/unified state error message and one case for a missing `schema_migrations` remediation message pointing to `scripts/apply_migrations.py`. Keep Redis and auth outside scope; this test is only about startup failure semantics.</action>
  <acceptance_criteria>
    - `rg -n 'mixed legacy|schema_migrations|apply_migrations.py|lifespan|create_app' apps/api/tests/integration/test_startup_manifest_rejection.py` finds the non-test startup rejection scenarios.
    - `cd apps/api && uv run pytest -q tests/integration/test_startup_manifest_rejection.py -o addopts=''` passes.
    - `cd apps/api && uv run pytest -q tests/unit/test_database_migration_guard.py tests/integration/test_startup_manifest_rejection.py -o addopts=''` passes as the plan gate.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && uv run pytest -q tests/unit/test_database_migration_guard.py tests/integration/test_startup_manifest_rejection.py -o addopts=''</automated>
  </verify>
  <done>Non-test lifespan startup demonstrably aborts on manifest guard failures, with explicit messages for missing migration history and mixed legacy/unified state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database physical state -> startup guard | Legacy tables or stray migration rows can make runtime behavior diverge from the active ORM/schema contract. |
| FastAPI lifespan -> request serving | If startup errors are swallowed, the app can serve requests against an invalid schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `apps/api/infrastructure/database.py` | mitigate | Reject mixed legacy/unified table state and unexpected manifest rows with explicit runtime errors before serving. |
| T-03-05 | D | `apps/api/main_v2.py` lifespan startup | mitigate | Keep `await init_db()` as a blocking startup precondition and prove failure propagation with lifespan integration tests. |
| T-03-06 | R | startup regression tests | mitigate | Add branch and integration coverage for mixed-state and missing-history rejection paths so future edits cannot relax them silently. |
</threat_model>

<verification>
Run `cd apps/api && uv run pytest -q tests/unit/test_database_migration_guard.py tests/integration/test_startup_manifest_rejection.py -o addopts=''`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Startup guard rejects manifest drift and mixed baseline state in non-test environments
- FastAPI lifespan does not swallow manifest guard failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-bootstrap/03-unified-bootstrap-02-SUMMARY.md`
</output>
